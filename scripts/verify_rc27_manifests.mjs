import fs from 'node:fs';import path from 'node:path';import crypto from 'node:crypto';const root=process.cwd();const sha=(file)=>crypto.createHash('sha256').update(fs.readFileSync(file)).digest('hex');
function verify(manifestPath,base){const manifest=JSON.parse(fs.readFileSync(path.join(root,manifestPath),'utf8')),failures=[];for(const entry of manifest.entries){const file=path.join(root,base,entry.path);if(!fs.existsSync(file)){failures.push({path:entry.path,reason:'missing'});continue;}const size=fs.statSync(file).size,digest=sha(file);if(size!==entry.size||digest!==entry.sha256)failures.push({path:entry.path,reason:'mismatch',expectedSize:entry.size,actualSize:size,expectedSha256:entry.sha256,actualSha256:digest});}return {manifestPath,expected:manifest.count,verified:manifest.entries.length-failures.length,failures,ok:failures.length===0};}
const full=verify('recovery/RC27_SHA256_MANIFEST.json',''),web=verify('recovery/RC27_WEB_SHA256_MANIFEST.json','dist/m26-cliente-premium-final-candidate');const report={release:'IBERFIT_M26_CLIENTE_PREMIUM_FINAL_RC27',version:'26.0.0-cliente-premium-final.27',generatedAt:new Date().toISOString(),full,web,ok:full.ok&&web.ok};fs.writeFileSync(path.join(root,'recovery','RC27_MANIFEST_VERIFICATION.json'),JSON.stringify(report,null,2)+'\n');console.log(`${full.ok?'PASS':'FAIL'} full ${full.verified}/${full.expected}`);console.log(`${web.ok?'PASS':'FAIL'} web ${web.verified}/${web.expected}`);if(!report.ok)process.exit(1);
