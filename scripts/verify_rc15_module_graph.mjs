import fs from 'node:fs';import path from 'node:path';
const root=path.join(process.cwd(),'dist','m26-launch-candidate');const entry=path.join(root,'m26','app.js');const visited=new Set(),missing=[];
const patterns=[/\b(?:import|export)\s+(?:[^'";]+?\s+from\s+)?['"]([^'"]+)['"]/g,/\bimport\(\s*['"]([^'"]+)['"]\s*\)/g];
function resolve(from,spec){if(spec.startsWith('node:')||spec.startsWith('http:')||spec.startsWith('https:'))return null;if(spec.startsWith('/'))return path.join(root,spec.slice(1));if(spec.startsWith('.'))return path.resolve(path.dirname(from),spec);return null;}
function walk(file){if(visited.has(file))return;visited.add(file);if(!fs.existsSync(file)){missing.push(path.relative(root,file));return;}const source=fs.readFileSync(file,'utf8');for(const regex of patterns){regex.lastIndex=0;for(let match;(match=regex.exec(source));){const target=resolve(file,match[1]);if(target)walk(target);}}}
walk(entry);const report={entry:path.relative(root,entry),modules:visited.size,missing:[...new Set(missing)].sort(),ok:missing.length===0};fs.writeFileSync(path.join(process.cwd(),'recovery','RC15_MODULE_GRAPH_REPORT.json'),JSON.stringify(report,null,2)+'\n');console.log(JSON.stringify(report,null,2));if(!report.ok)process.exit(1);
