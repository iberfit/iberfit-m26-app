const MISSING=/PGRST202|not find the function|M26_HTTP_404/i;
export function createCommunicationTransport({runtime,fetchImpl=globalThis.fetch}={}){const url=new URL(String(runtime?.url||''));if(url.protocol!=='https:'&&!['localhost','127.0.0.1'].includes(url.hostname))throw new Error('M26_COMMUNICATION_HTTPS_REQUIRED');const key=String(runtime?.publishableKey||runtime?.anonKey||'');async function rpc(name,token,params={}){const response=await fetchImpl(`${url.origin}/rest/v1/rpc/${name}`,{method:'POST',credentials:'omit',cache:'no-store',redirect:'error',referrerPolicy:'no-referrer',headers:{apikey:key,authorization:`Bearer ${token}`,'content-type':'application/json','x-client-info':`iberfit-m26-communication/${runtime?.version||'26.0.0'}`},body:JSON.stringify(params)});const body=response.status===204?null:(response.headers?.get?.('content-type')||'').includes('json')?await response.json().catch(()=>({})):await response.text().catch(()=>'');if(!response.ok){const error=new Error(body?.message||body?.error||`M26_HTTP_${response.status}`);error.status=response.status;throw error;}return Array.isArray(body)&&body.length===1?body[0]:body;}
  return Object.freeze({bootstrapOptional:async(token,{application}={})=>{if(!['client','coach'].includes(application))return Object.freeze({available:false,reason:'unsupported',data:null});try{const data=await rpc('iberfit_communication_bootstrap_v14',token,{p_application:application});return Object.freeze({available:true,reason:null,data});}catch(error){if(error?.status===404||MISSING.test(String(error?.message||error)))return Object.freeze({available:false,reason:'missing',data:null});throw error;}},execute:async(token,command,{application}={})=>{const result=await rpc('iberfit_communication_execute_v14',token,{p_application:application,p_command:command});if(result?.ok!==true||!['ack','duplicate'].includes(String(result?.kind||'').toLowerCase()))throw new Error('M26_COMMUNICATION_MUTATION_NOT_CONFIRMED');return result;}});
}
