const e=(v)=>String(v??'').replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;').replaceAll("'",'&#039;');
function thread(vm,t){return `<details class="m26-thread"><summary><strong>${e(vm.role==='client'?t.coachName||'Tu Coach':t.clientName||'Cliente')}</strong><span>${e(t.unreadCount||0)} nuevos</span></summary><div class="m26-thread-body">${t.messages.length?t.messages.map((m)=>`<article class="m26-message is-${e(m.senderRole)}"><strong>${e(m.senderRole==='client'?'Cliente':'Coach')}</strong><p>${e(m.body)}</p><small>${e(m.createdAt||'')}</small></article>`).join(''):'<p>Sin mensajes todavía.</p>'}<form data-communication-form="message-send"><input type="hidden" name="threadId" value="${e(t.id)}"><textarea name="body" maxlength="4000" required placeholder="Escribe un mensaje…"></textarea><button type="submit">Enviar</button></form><form data-communication-form="thread-read"><input type="hidden" name="threadId" value="${e(t.id)}"><button type="submit">Marcar como leída</button></form></div></details>`;}
export function renderCommunicationRoute(vm){if(!vm?.communication)return null;if(vm.kind==='communication-unavailable')return `<div class="m26-communication-route"><section class="m26-communication-hero"><h2>Mensajes no disponibles</h2><p>El backend de comunicación no está instalado o no respondió.</p></section></div>`;const open=vm.canOpenThread?`<form data-communication-form="thread-open" class="m26-communication-panel"><h3>Abrir conversación</h3><select name="clientId" required><option value="">Cliente</option>${vm.clients.map((c)=>`<option value="${e(c.id)}">${e(c.name||c.id)}</option>`).join('')}</select><input name="subject" value="Seguimiento IBERFIT"><button type="submit">Abrir</button></form>`:'';return `<div class="m26-communication-route"><section class="m26-communication-hero"><h2>Mensajes</h2><p>Comunicación privada Cliente–Coach dentro de IBERFIT.</p></section>${open}<section class="m26-communication-grid"><article class="m26-communication-panel"><h3>Conversaciones</h3>${vm.threads.length?vm.threads.map((t)=>thread(vm,t)).join(''):'<p>Sin conversaciones.</p>'}</article><aside class="m26-communication-panel"><h3>Notificaciones</h3>${vm.notifications.length?vm.notifications.map((n)=>`<article><strong>${e(n.title||'IBERFIT')}</strong><p>${e(n.body||'')}</p>${!n.readAt?`<form data-communication-form="notification-read"><input type="hidden" name="notificationId" value="${e(n.id)}"><button type="submit">Marcar leída</button></form>`:''}</article>`).join(''):'<p>Sin avisos.</p>'}</aside></section></div>`;}
