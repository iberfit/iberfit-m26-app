import {messagesForThread} from './state.js';
export function createCommunicationRouteViewModel(base,shellVm,state){const area=String(shellVm?.activeArea||state?.activeArea||'');const role=String(shellVm?.identity?.role||state?.identity?.role||'').toLowerCase();if(area!=='mensajes'||!['client','coach'].includes(role))return base;if(state?.communication?.available!==true)return Object.freeze({...base,communication:true,kind:'communication-unavailable',reason:state?.communication?.reason||'backend_unavailable',role});return Object.freeze({...base,communication:true,kind:'communication',role,threads:Object.freeze((state.communication.threads||[]).map((t)=>Object.freeze({...structuredClone(t),messages:messagesForThread(state,t.id)}))),notifications:Object.freeze(structuredClone(state.communication.notifications||[])),clients:role==='coach'?Object.freeze(structuredClone(state.collections?.clients||[])):Object.freeze([]),canOpenThread:role==='coach'});}
