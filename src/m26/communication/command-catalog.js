const COMMANDS=Object.freeze({MESSAGE_THREAD_OPEN:{roles:['coach'],body:false},MESSAGE_SEND:{roles:['coach','client'],body:true},MESSAGE_MARK_READ:{roles:['coach','client'],body:false},NOTIFICATION_MARK_READ:{roles:['coach','client'],body:false}});
function op(){return globalThis.crypto?.randomUUID?.()||`COM-${Date.now()}-${Math.random().toString(16).slice(2)}`;}
export function createCommunicationCommand(input={},state={},role=''){const type=String(input.type||'').toUpperCase();const def=COMMANDS[type];if(!def)throw new Error('M26_COMMUNICATION_COMMAND_NOT_REGISTERED');if(!def.roles.includes(String(role).toLowerCase()))throw new Error('M26_COMMUNICATION_ROLE_FORBIDDEN');if(state?.available!==true)throw new Error('M26_COMMUNICATION_BACKEND_UNAVAILABLE');const payload=structuredClone(input.payload||{});if(def.body){payload.body=String(payload.body||'').replace(/[\u0000-\u001f\u007f]/g,' ').trim();if(!payload.body||payload.body.length>4000)throw new Error('M26_COMMUNICATION_BODY_INVALID');}return Object.freeze({operationId:String(input.operationId||op()),type,entityId:String(input.entityId||''),baseRevision:Number(input.baseRevision||0),payload:Object.freeze(payload),onlineOnly:true,createdAt:new Date().toISOString()});}
export const M26_COMMUNICATION_COMMAND_TYPES=Object.freeze(Object.keys(COMMANDS));
