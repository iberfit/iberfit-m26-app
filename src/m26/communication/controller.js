const toast=(message)=>{try{globalThis.dispatchEvent(new CustomEvent('m26:toast',{detail:{message}}));}catch{}};
const text=(d,k,m=4000)=>String(d.get(k)||'').replace(/[\u0000-\u001f\u007f]/g,' ').trim().slice(0,m);
export function createCommunicationController({root,service,render=()=>{}}={}){let busy=false;async function run(input,msg){if(busy)return;busy=true;try{await service.execute(input);toast(msg);render();}catch(error){toast(/ONLINE_REQUIRED/.test(String(error?.message||error))?'Los mensajes requieren conexión.':'No fue posible confirmar la comunicación.');throw error;}finally{busy=false;}}async function onSubmit(event){const form=event.target.closest?.('[data-communication-form]');if(!form)return;event.preventDefault();const d=new FormData(form);const kind=form.dataset.communicationForm;if(kind==='thread-open'){const clientId=text(d,'clientId',200);await run({type:'MESSAGE_THREAD_OPEN',entityId:clientId,payload:{clientId,subject:text(d,'subject',160)}},'Conversación abierta.');}else if(kind==='message-send'){const threadId=text(d,'threadId',200);await run({type:'MESSAGE_SEND',entityId:threadId,payload:{threadId,body:text(d,'body')}},'Mensaje enviado.');form.reset();}else if(kind==='thread-read'){const threadId=text(d,'threadId',200);await run({type:'MESSAGE_MARK_READ',entityId:threadId,payload:{threadId}},'Conversación actualizada.');}else{const notificationId=text(d,'notificationId',200);await run({type:'NOTIFICATION_MARK_READ',entityId:notificationId,payload:{notificationId}},'Notificación leída.');}}return Object.freeze({mount(){root.addEventListener('submit',onSubmit);},destroy(){root.removeEventListener('submit',onSubmit);}});}
