import {createAdminCommand} from './command-catalog.js';
function canonical(value){if(Array.isArray(value))return value.map(canonical);if(value&&typeof value==='object')return Object.fromEntries(Object.keys(value).sort().map((k)=>[k,canonical(value[k])]));return value;}
function fingerprint(command){return JSON.stringify(canonical({type:command.type,entityId:command.entityId,organizationId:command.organizationId,baseRevision:command.baseRevision,reason:command.reason,payload:command.payload}));}
export function createAdminCommandService({transport,getToken,getAdminState,isOnline=()=>true,refreshState=async()=>{}}={}){if(!transport?.execute)throw new Error('M26_ADMIN_TRANSPORT_REQUIRED');const inFlight=new Map();return Object.freeze({execute(input){if(!isOnline())return Promise.reject(new Error('M26_ADMIN_ONLINE_REQUIRED'));const command=createAdminCommand(input,getAdminState());const signature=fingerprint(command);const current=inFlight.get(command.operationId);if(current){if(current.signature!==signature)return Promise.reject(new Error('M26_ADMIN_OPERATION_ID_COLLISION'));return current.promise;}const promise=(async()=>{const response=await transport.execute(await getToken(),command);await refreshState({reason:'admin-mutation-ack',response});return Object.freeze({ok:true,command,response});})().finally(()=>inFlight.delete(command.operationId));inFlight.set(command.operationId,{signature,promise});return promise;}});}
export const __adminServiceInternals=Object.freeze({canonical,fingerprint});
